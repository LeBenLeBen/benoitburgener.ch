<footer class="site-footer pt-10 pb-8">
  <div class="container">
    <div class="flex flex-col sm:flex-row items-center justify-between gap-y-4 gap-x-8 max-xs:text-center text-gray-600">
      <div class="text-sm">
        Copyright © {{ site.currentYear }}, {{ site.author.name }} &middot; <a href="/impressum/">Impressum</a>
      </div>

      <ul class="flex justify-between">
        {% for link in links %}
          <li class="ml-5 md:ml-6 lg:ml-8 first:ml-0">
            <a href="{{ link.url }}" aria-label="{{ link.label }}" title="{{ link.label }}" class="block" rel="me">
              {{ icon(link.icon, 'icon--100 md:icon--150') }}
            </a>
          </li>
        {% endfor %}
      </ul>
    </div>
  </div>
</footer>
